/*
package widgets

import (
    "testing"

    "github.com/stretchr/testify/assert"
)

func TestGetVal(t *testing.T) {
    //ctx, err := yh.NewContextFromSc()
    //assert.NoError(t, err)

    sv := NewStatic(1)
    assert.Equal(t, 1, sv.GetVal(10))

    sv = NewFrac(0.5)
    assert.Equal(t, 5, sv.GetVal(10))

    sv = NewFrac(0.5).PlusStatic(1)
    assert.Equal(t, 6, sv.GetVal(10))

}
*/
